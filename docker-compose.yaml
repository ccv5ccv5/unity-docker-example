version: '3.8'

services:
  webgl:
    container_name: unity-webgl-demo
    image: unity-webgl-demo
    build: .
    ports:
      - "8765:443"
    volumes:
      - ./webgl:/usr/share/nginx/html
      - ./ssl:/etc/nginx/ssl